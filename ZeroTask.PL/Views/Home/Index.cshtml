@using ZeroTask.PL.Models;
@{
    ViewData["Title"] = "Eventing";
}
@model List<UserEventViewModel>

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>This is a "0" Task for c# next course=)</p>
</div>
<div class="d-flex flex-row">
    <a class="btn btn-success" asp-action="Create">Create event</a>
</div>

<table class="table">
    <tr>
        <th>
            Name
        </th>
        <th>
            Category
        </th>
        <th>
            Description
        </th>
        <th>
            Place
        </th>
        <th>
            Time
        </th>
        <th>
            Date
        </th>
        <th>
            AdditionalInfo
        </th>
        <th>
            ImageUrl
        </th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @item.Name
            </td>
            <td>
                @item.Category
            </td>
            <td>
                @item.Description
            </td>
            <td>
                @item.Place
            </td>
            <td>
                @item.Time.ToShortTimeString()
            </td>
            <td>
                @item.Date.ToShortDateString()
            </td>
            <td>
                @item.AdditionalInfo
            </td>
            <td>
                @item.ImageUrl
            </td>
            <td>
                <div class="btn-group">
                    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                </div>

            </td>
        </tr>
    }
</table>
